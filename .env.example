# TraceNet Blockchain - Environment Variables EXAMPLE
# Copy this file to .env and fill in your values

# Server Configuration
NODE_ENV=development
PORT=3000

# JWT Authentication
# IMPORTANT: Generate a strong secret for production!
# Use: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Configuration
# Allow local dev and your public URL
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# Blockchain Configuration
GENESIS_REWARD=1000000
BLOCK_TIME=10000
MAX_TRANSACTIONS_PER_BLOCK=100

# Logging
LOG_LEVEL=info

# Rate Limiting (requests per 15 minutes)
RATE_LIMIT_MAX=100

# --- Node Specific (Added by GUI) ---
# HOST: 0.0.0.0 to allow external connections, 127.0.0.1 for local only
HOST=0.0.0.0

# PEERS: Comma-separated list of secure bootstrap nodes
PEERS=https://tracenet-blockchain-136028201808.us-central1.run.app

# PUBLIC_HOST: Your external IP or Domain (e.g., ngrok URL)
# Needed for other nodes to connect back to you
PUBLIC_HOST=http://your-public-ip-or-domain.com

# NODE_WALLET_PRIVATE_KEY: Your node's mining wallet key
NODE_WALLET_PRIVATE_KEY=your_private_key_here
